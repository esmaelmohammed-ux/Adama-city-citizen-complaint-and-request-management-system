{
  "name": "adama-citizen-portal",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node scripts/start-production.js",
    "build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend && npm install --prefix ai-service"
  }
}
